# CC-P0-01 — Nerdy app identity and Quest launcher

**Status:** draft / awaiting owner review · **Type:** HITL · **Scope size:** M
The owner opens Quest Library, sees "Nerdy" with the Nerdy logo, and cold-launches the app without ADB.

## Copy-Paste This Into New Agent

```text
Work only in /Users/jad/Desktop/math-workbench-airlift, branch unity-airlift.
Read docs/00-build/PHASE-0-NERDY-WELCOME.md, AI-VOICE-GUIDE.md, constraints.md,
TESTING.md and this ticket. Confirm owner approval before implementation.
I'm working on CC-P0-01: Nerdy app identity and Quest launcher.
Record actual starting state (git status, evidence, uncommitted ownership) before changes.
Goal: The owner opens Quest Library, sees "Nerdy" with the Nerdy logo, and cold-launches the app without ADB.
Implement this ticket only; run its tests and physical checks; record results in DEV-LOG.
No push, purchase, child data or provider spend beyond the approved key/budget.
```

## Context Summary for Agent

### What the previous ticket delivered (at start)

Record actual files, evidence, artifact/hash and branch state here when starting.

### Ticket Scope

- Phase: 0 — Nerdy welcome. Scope size: M. No speculative time estimate.
- Dependencies: None (first Phase 0 ticket). Absorbs CC-P1-02.5.
- Branch: existing unity-airlift; no automatic branch/merge.
- Parent: PHASE-0-NERDY-WELCOME.md; product contract and pedagogical invariants unchanged.

### Acceptance criteria

- [ ] App product name "Nerdy", company "Nerdy", Android package `com.nerdy.vr`, version 0.2.0; the old `com.jad.airlift` package is uninstalled from the Quest.
- [ ] Owner-supplied logo is the adaptive launcher icon (foreground/background) and legacy icon; the rights assertion is recorded in CC-P0-08.
- [ ] Quest Library route documented and verified on this OS build (Unknown Sources filter or wherever it actually appears); the owner finds and launches it without the Mac.
- [ ] Build wrapper, install scripts, docs and memory reference the new package id; APK scan and checks still pass.

### Implementation details

Set ProjectSettings productName/companyName/applicationIdentifier; import the logo as Android adaptive icon set; verify the merged AndroidManifest label and VR intent categories; update scripts/verify_cargo.py and docs; uninstall the old package once.

### Key constraints

Deterministic math only in C#; the guide never grades, moves pieces or advances stages.
No names, images, account or persistent device identifiers; no audio or transcripts
persisted by our proxy; adult testers only until CC-P0-08 records a child-use go.
Style: owner's Live Learning Style Guide tokens. No purchases beyond the approved key.

### Files to modify

- unity/ProjectSettings/ProjectSettings.asset
- scripts/verify_cargo.py
- docs/qa/cargo-CC-P1-02.md
- CLAUDE.md
- AGENTS.md

### Files to create

- unity/Assets/Airlift/Branding/nerdy-logo.png (owner asset)
- unity/Assets/Airlift/Tests/EditMode/AppIdentityTests.cs

### Testing requirements

AppIdentityTests: product name, package id, icon assigned for Android. Physical: owner finds and cold-launches from Library; note the exact menu path.

### Common gotchas

A package id change is a fresh install (no upgrade). Sideloaded apps do not appear on the main dashboard like store apps. Unity Android icon slots must all be filled or the default icon shows.

### Definition of Done

Code-complete with named tests passing is not acceptance. Owner/device acceptance is
recorded in DEV-LOG with build hash and evidence. Commit only after acceptance; no push.

### Why (fill at completion)

