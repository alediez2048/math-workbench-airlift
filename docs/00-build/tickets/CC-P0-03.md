# CC-P0-03 — Realtime voice spike on Quest (throwaway)

**Status:** draft / awaiting owner review · **Type:** HITL · **Scope size:** M
Answer with numbers whether two-way live voice with the OpenAI Realtime API works on this Quest 3S inside the Meta SDK app.

## Copy-Paste This Into New Agent

```text
Work only in /Users/jad/Desktop/math-workbench-airlift, branch unity-airlift.
Read docs/00-build/PHASE-0-NERDY-WELCOME.md, AI-VOICE-GUIDE.md, constraints.md,
TESTING.md and this ticket. Confirm owner approval before implementation.
I'm working on CC-P0-03: Realtime voice spike on Quest (throwaway).
Record actual starting state (git status, evidence, uncommitted ownership) before changes.
Goal: Answer with numbers whether two-way live voice with the OpenAI Realtime API works on this Quest 3S inside the Meta SDK app.
Implement this ticket only; run its tests and physical checks; record results in DEV-LOG.
No push, purchase, child data or provider spend beyond the approved key/budget.
```

## Context Summary for Agent

### What the previous ticket delivered (at start)

Record actual files, evidence, artifact/hash and branch state here when starting.

### Ticket Scope

- Phase: 0 — Nerdy welcome. Scope size: M. No speculative time estimate.
- Dependencies: OpenAI key with Realtime access; adult tester. Independent of P0-01/02.
- Branch: existing unity-airlift; no automatic branch/merge.
- Parent: PHASE-0-NERDY-WELCOME.md; product contract and pedagogical invariants unchanged.

### Acceptance criteria

- [ ] A throwaway scene connects over WSS with an ephemeral client secret minted by a minimal dev proxy; mic audio (24 kHz PCM16) streams up; guide audio streams back and plays; captions show transcript deltas.
- [ ] Round-trip latency (end of learner speech to first guide audio) measured over 10 exchanges; 5-minute session stability; behavior on Wi-Fi drop recorded.
- [ ] Transport and library decision recorded (System.Net.WebSockets vs a pinned NativeWebSocket; WebSocket vs WebRTC) with the reasons.
- [ ] Nothing from the spike ships unless promoted explicitly by CC-P0-04; no key in the repo or APK.

### Implementation details

Minimal Unity scene: Microphone capture → resample → base64 audio chunks; receive audio deltas → ring buffer → AudioSource; local proxy script for the ephemeral secret. Record measurements in DEV-LOG.

### Key constraints

Deterministic math only in C#; the guide never grades, moves pieces or advances stages.
No names, images, account or persistent device identifiers; no audio or transcripts
persisted by our proxy; adult testers only until CC-P0-08 records a child-use go.
Style: owner's Live Learning Style Guide tokens. No purchases beyond the approved key.

### Files to modify

- none in shipped scenes

### Files to create

- unity/Assets/Airlift/Spike/RealtimeSpike.cs (throwaway)
- services/guide-proxy/dev-mint.ts (throwaway)

### Testing requirements

No automated tests; measurements and a go/no-go note are the deliverable.

### Common gotchas

Quest mic requires RECORD_AUDIO permission and a runtime prompt. IL2CPP + TLS WebSockets need testing on device, not in the editor. Audio output must use the XR audio device; check sample-rate conversion artifacts.

### Definition of Done

Code-complete with named tests passing is not acceptance. Owner/device acceptance is
recorded in DEV-LOG with build hash and evidence. Commit only after acceptance; no push.

### Why (fill at completion)

