# CC-P0-04 — Guide service: proxy and Unity GuideSession

**Status:** draft / awaiting owner review · **Type:** HITL · **Scope size:** XL
A private serverless proxy mints budgeted ephemeral sessions; Unity holds one guide session with mic streaming, playback, captions, tool calls, structured context and an offline fallback.

## Copy-Paste This Into New Agent

```text
Work only in /Users/jad/Desktop/math-workbench-airlift, branch unity-airlift.
Read docs/00-build/PHASE-0-NERDY-WELCOME.md, AI-VOICE-GUIDE.md, constraints.md,
TESTING.md and this ticket. Confirm owner approval before implementation.
I'm working on CC-P0-04: Guide service: proxy and Unity GuideSession.
Record actual starting state (git status, evidence, uncommitted ownership) before changes.
Goal: A private serverless proxy mints budgeted ephemeral sessions; Unity holds one guide session with mic streaming, playback, captions, tool calls, structured context and an offline fallback.
Implement this ticket only; run its tests and physical checks; record results in DEV-LOG.
No push, purchase, child data or provider spend beyond the approved key/budget.
```

## Context Summary for Agent

### What the previous ticket delivered (at start)

Record actual files, evidence, artifact/hash and branch state here when starting.

### Ticket Scope

- Phase: 0 — Nerdy welcome. Scope size: XL. No speculative time estimate.
- Dependencies: CC-P0-03 go; CC-P0-02 for captions UI.
- Branch: existing unity-airlift; no automatic branch/merge.
- Parent: PHASE-0-NERDY-WELCOME.md; product contract and pedagogical invariants unchanged.

### Acceptance criteria

- [ ] `POST /session` returns an ephemeral Realtime client secret bound to a server-authored session config; rejects missing/invalid launch nonce; enforces per-session and daily budgets; long-lived key only in environment; no audio/transcript logging.
- [ ] Unity `GuideSession`: connect, stream mic, play audio, captions, tool-call dispatch to C# handlers, `GuideContext` push, reconnect once, graceful end.
- [ ] `LocalGuide` fallback engages when offline, denied or over budget; the UI labels it "offline guide" and never presents scripted captions as live AI.
- [ ] Contract tests for the proxy; EditMode tests for message schema validation, context PII rejection and fallback routing; APK credential scan passes.

### Implementation details

TypeScript proxy (Vercel Functions preferred). Unity: NativeWebSocket or System.Net.WebSockets per spike; audio ring buffers; JSON schema for tool payloads; GuideContext builder from lesson state.

### Key constraints

Deterministic math only in C#; the guide never grades, moves pieces or advances stages.
No names, images, account or persistent device identifiers; no audio or transcripts
persisted by our proxy; adult testers only until CC-P0-08 records a child-use go.
Style: owner's Live Learning Style Guide tokens. No purchases beyond the approved key.

### Files to modify

- scripts/verify_cargo.py (scan patterns)
- docs/00-build/SERVER-VERIFICATION.md
- docs/00-build/AI-VOICE-GUIDE.md

### Files to create

- services/guide-proxy/{package.json,api/session.ts,schema.ts,test/*.test.ts}
- unity/Assets/Airlift/Scripts/Guide/{GuideSession.cs,GuideContext.cs,GuideTools.cs,LocalGuide.cs,MicStreamer.cs,AudioPlayback.cs}
- unity/Assets/Airlift/Tests/EditMode/{GuideContextTests.cs,GuideRoutingTests.cs}

### Testing requirements

`npm test` in services/guide-proxy; EditMode GuideContextTests and GuideRoutingTests; device check of connect/speak/listen with the adult tester.

### Common gotchas

Never send names, audio to our own server, or unsubmitted correctness. Ephemeral secrets expire quickly; mint on launch, not at build. Budget by session nonce, not device id.

### Definition of Done

Code-complete with named tests passing is not acceptance. Owner/device acceptance is
recorded in DEV-LOG with build hash and evidence. Commit only after acceptance; no push.

### Why (fill at completion)

