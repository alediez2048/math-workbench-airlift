# CC-P0-07 — Guide inside the Cargo workbench

**Status:** draft / awaiting owner review · **Type:** HITL · **Scope size:** L
Entering Cargo Crew, the guide gives a short tour, reacts to committed actions with code-supplied facts, and answers lesson questions by voice within an allowlisted scope.

## Copy-Paste This Into New Agent

```text
Work only in /Users/jad/Desktop/math-workbench-airlift, branch unity-airlift.
Read docs/00-build/PHASE-0-NERDY-WELCOME.md, AI-VOICE-GUIDE.md, constraints.md,
TESTING.md and this ticket. Confirm owner approval before implementation.
I'm working on CC-P0-07: Guide inside the Cargo workbench.
Record actual starting state (git status, evidence, uncommitted ownership) before changes.
Goal: Entering Cargo Crew, the guide gives a short tour, reacts to committed actions with code-supplied facts, and answers lesson questions by voice within an allowlisted scope.
Implement this ticket only; run its tests and physical checks; record results in DEV-LOG.
No push, purchase, child data or provider spend beyond the approved key/budget.
```

## Context Summary for Agent

### What the previous ticket delivered (at start)

Record actual files, evidence, artifact/hash and branch state here when starting.

### Ticket Scope

- Phase: 0 — Nerdy welcome. Scope size: L. No speculative time estimate.
- Dependencies: CC-P0-04, CC-P0-06.
- Branch: existing unity-airlift; no automatic branch/merge.
- Parent: PHASE-0-NERDY-WELCOME.md; product contract and pedagogical invariants unchanged.

### Acceptance criteria

- [ ] Tour covers handle, strap, pad and ruler from a versioned fact catalog; Help re-asks the current step; mute and captions toggles work.
- [ ] `GuideContext` is pushed on grab, place, split, submit (with verdict) and reset; the guide never states unsubmitted correctness and never moves pieces or advances stages.
- [ ] Learner questions outside the lesson scope get a friendly redirect; injection attempts in speech cannot change lesson state (tools have no state-changing verbs).
- [ ] GuideContextTests (no PII, verdict gating), CoreAIGuideTests (event → context mapping), physical check with the live provider by an adult tester; offline fallback labeled.

### Implementation details

CargoLessonDirector raises events → GuideContext builder → GuideSession; fact catalog ScriptableObject; scope allowlist in the session instructions plus client-side tool whitelist.

### Key constraints

Deterministic math only in C#; the guide never grades, moves pieces or advances stages.
No names, images, account or persistent device identifiers; no audio or transcripts
persisted by our proxy; adult testers only until CC-P0-08 records a child-use go.
Style: owner's Live Learning Style Guide tokens. No purchases beyond the approved key.

### Files to modify

- unity/Assets/Airlift/Scripts/Lessons/CargoLessonDirector.cs
- unity/Assets/Airlift/Scripts/Onboarding/OnboardingDirector.cs

### Files to create

- unity/Assets/Airlift/Scripts/Guide/{CargoGuideFacts.asset,GuideContextBuilder.cs}
- unity/Assets/Airlift/Tests/EditMode/{CoreAIGuideTests.cs}

### Testing requirements

GuideContextTests, CoreAIGuideTests, existing lesson suites; device: tour, reaction to split/submit, one question answered, one off-scope redirect.

### Common gotchas

Impact-check CargoLessonDirector before editing (its grab flows are the accepted core loop). Do not let the guide narrate during a held grab in a way that blocks Submit.

### Definition of Done

Code-complete with named tests passing is not acceptance. Owner/device acceptance is
recorded in DEV-LOG with build hash and evidence. Commit only after acceptance; no push.

### Why (fill at completion)

