# CC-P4-02 — Add optional spoken questions to the core guide

**Status:** draft / awaiting owner review · **Type:** HITL · **Scope size:** L
After scoped eligibility and spending approvals, an adult can ask spoken lesson questions through the existing private guide backend without keys in the APK.

## Copy-Paste This Into New Agent

```text
Work only in /Users/jad/Desktop/math-workbench-airlift, branch unity-airlift.
Read docs/00-build/{PRD,CONTEXT,systemsdesign,techstack,requirements,constraints,TESTING,TICKETS}.md
and docs/00-build/tickets/CC-P4-02.md. Confirm owner approval before implementation.
I'm working on CC-P4-02: Add optional spoken questions to the core guide.
Current state (at start): inspect git status and the preceding ticket's real evidence;
record files, test results, artifact and uncommitted ownership before modifying anything.
What's NOT done yet: this ticket's acceptance is unverified; later tickets remain pending.
Goal: After scoped eligibility and spending approvals, an adult can ask spoken lesson questions through the existing private guide backend without keys in the APK.
Inspect unity/Assets/Airlift/Scripts/Presentation/IGuidePlayer.cs before starting. Preserve unrelated work.
Follow D1–D10 and F-18. Use the Game Developer and 3D Interaction Design skills.
Implement this ticket only. Run named tests and physical checks, record actual results
and mirror completion rationale into DEV-LOG.md. Stop for owner review before the next ticket.
Do not automatically push, merge, submit, purchase services or collect child data.
```

## Context Summary for Agent

### What the previous ticket delivered (at start)

Dependency: CC-P3-06 accepted plus CC-P4-01 go and separate privacy/account/budget approval.
Record actual created/modified files, infrastructure (if any), artifact/hash,
verification failures and current branch state here when starting. This field is
intentionally factual-at-start; it is not prefilled with fictional completed work.
Baseline today is recorded in ../CLAUDE-CODEX.md; planned dependency output is not evidence.

### CC-P4-02 Scope

- Phase: 4 — Gated spoken questions and child-release readiness.
- Scope size: L; replaces the template's time budget. No speculative time estimate.
- Dependencies: CC-P3-06 accepted plus CC-P4-01 go and separate privacy/account/budget approval.
- Branch: existing unity-airlift; do not create/merge a branch automatically.
- Requirements: F-18.
- User stories: US14.
- Additional gate: provider eligibility and any required privacy/account/budget approvals. CC-P4-01 may conclude no-go; then later Phase 4 tickets remain blocked.

### Acceptance criteria

- [ ] This ticket extends the P1-03 core AI guide with optional spoken questions; it cannot redefine AI output as a future-only feature. Child use remains independently gated.

- [ ] No execution unless P4-01 go plus explicit account/budget/privacy authorization.
- [ ] Server mints limited session credential; APK contains no provider key and unauthorized mint requests fail.
- [ ] Actual Quest audio capture/playback, cancellation and offline fallback are measured, not inferred from WebSocket docs.
- [ ] F-18 verified against requirements.md; actual results linked, not inferred from compilation.

### Implementation details

1. Extend existing P1-03 guide backend with a VoiceSession microphone adapter and scoped session issuer; pin evaluated transport dependency only after review.
2. Use synthetic adult-authored test speech; redact tokens/logs and stop mic on Back/pause.
3. Measure turn latency and failure rate; reject integration if it harms the core output-only AI guide.
4. Preserve the complete offline lesson; a blocked/no-go outcome is valid and does not authorize a workaround.

### Key constraints

1. Planning draft; implementation requires owner approval and dependency acceptance.
2. D2/D3: exact same-whole quantities, released structural swaps, idempotent commands.
3. D6/D7: wrong valid independent work stays editable; captions/local guide work offline.
4. D10: no child data, embedded credentials, unapproved ElevenLabs assets or purchases.
5. Preserve DeviceProof, Onboarding reference and unrelated changes; no automatic Git merge.

### Files to modify

- `services/voice-session/` — accepted P1-03 service, extended with authorized optional speech input.

- `unity/Assets/Airlift/Scripts/Presentation/IGuidePlayer.cs` — inspect before changing; action is limited to the implementation details above.
Current contents (at start): inspect and record relevant symbols/serialized references.
Paths introduced by prior tickets must actually exist before this ticket starts.

### Files to create

- `unity/Assets/Airlift/Scripts/Guidance/VoiceSession.cs` — ticket-specific implementation/content/evidence described above.
- `docs/qa/spoken-question-contract.md` — added input/session schema and measured adult-only transport limits.
- `docs/qa/voice-spike.md` — ticket-specific implementation/content/evidence described above.
- `unity/Assets/Airlift/Tests/EditMode/VoiceSessionContractTests.cs` — named assertions below.
- `unity/Assets/Airlift/Tests/PlayMode/VoiceSessionContractTestsSceneTests.cs` — this ticket's visible behavior, interruption/error recovery and no premature advancement.
- `docs/qa/cargo-CC-P4-02.md` — scrubbed result, commands, device checks and artifact link.
- Commit Unity-generated .meta files for new Unity assets; never hand-invent GUID reuse.

### Config / schema / store updates

Only explicitly approved voice gate/session fields in D10; no student store or implicit microphone enablement.
Register VoiceSessionContractTests in the verification wrapper; add required integration checks.
If the schema changes, include fixture compatibility/reset tests before runtime consumers.

### Server and gate enforcement

Extend P1-03 VoiceEligibilityGateTests for microphone-specific scope; P4-01 remains documentary.
Implement both actual issuer contract tests and Unity gate checks per SERVER-VERIFICATION.md.
```sh
/opt/homebrew/opt/node@22/bin/node /opt/homebrew/opt/node@22/lib/node_modules/npm/bin/npm-cli.js --prefix services/voice-session ci
/opt/homebrew/opt/node@22/bin/node /opt/homebrew/opt/node@22/lib/node_modules/npm/bin/npm-cli.js --prefix services/voice-session test
/opt/homebrew/opt/node@22/bin/node /opt/homebrew/opt/node@22/lib/node_modules/npm/bin/npm-cli.js --prefix services/voice-session run test:integration
```
Scripts/package/lock already originate in P1-03; this ticket extends them. Before any adult spike record
exact auth, token/session lifetime, rate/spend limits and fail-closed policy configuration.
No public deployment is implicitly authorized.

### Testing requirements

Automated:
```sh
bash scripts/verify-cargo.sh --suite VoiceSessionContractTests --build
git diff --check
```
The wrapper is a **planned CC-P1-01 deliverable**, not an existing command today.
For CC-P1-01 bootstrap, follow TESTING.md underlying CLI commands before testing the wrapper.

Named cases: `UnauthorizedMintDenied`, `SecretAbsentFromClient`, `PauseStopsMic`, `OfflineUsesLocalCue`.
Keep existing OnboardingFlowTests green. Include an error/cancellation case and
scene-level check; passing pure-state tests alone does not prove XR behavior.

Manual:
- [ ] Authorized adult-only Quest spike; no child-targeted rollout implied by a successful transport test.
- [ ] Current and rollback scenes remain usable; captions/mute and relevant single-controller path checked.
- [ ] Required provider, privacy and owner approvals recorded before any network/mic experiment.

Eval: Use approved adult/synthetic cases only; record measured outcomes and no-go conditions.
Update docs: change status only to what evidence supports; add DEV-LOG entry and identical Why paragraph.

### Reference

- [Canonical style guide](../STYLE-GUIDE.md), [core AI guide](../AI-VOICE-GUIDE.md). Apply only requirements introduced by this ticket or accepted predecessors; baseline reproduction does not require future style/audio assets.

- [Product requirements](../PRD.md), [testable requirements](../requirements.md).
- [Design D1–D10](../systemsdesign.md), [storyboard](../STORYBOARD.md), [testing](../TESTING.md).
- [Primary-source revision evidence](../../plans/2026-09-13-unity-vet/fraction-expansion-research.md).
- Read-only source methodology: /Users/jad/Desktop/LabelCheck/docs/00-build/TICKET-TEMPLATE.md.

### Common gotchas

1. A paid plan or adult test does not clear restrictions on a child-targeted product.
2. Rendered scale/notation is not the authoritative fraction quantity.
3. Headset acceptance, audio rights and owner approval cannot be replaced by a green editor test.

### Definition of Done

Code complete when:
- [ ] Ticket acceptance behavior implemented and named automated tests pass.
- [ ] No compilation errors; warnings investigated or recorded with release impact.
- [ ] Relevant offline, recovery, accessibility and secret-handling constraints pass.

Ticket complete when:
- [ ] Code-complete criteria met plus manual/device checks recorded and owner accepted.
- [ ] TICKETS.md, evidence and DEV-LOG.md updated; Why mirrored verbatim.
- [ ] Reviewed Git checkpoint recorded if owner authorizes commit; no automatic push/merge.
- [ ] Remaining scope clearly distinguished from this ticket; next work waits for review.

### Expected output

After scoped eligibility and spending approvals, an adult can ask spoken lesson questions through the existing private guide backend without keys in the APK.
Owner receives a specific observable result and evidence, not a claim that later
tickets or the entire lesson are complete.

### Dependencies to install

None now. Any future transport/server dependency requires gate clearance, license review and explicit approval; record exact pinned choice from the adult spike.

### Why (fill at completion)

Write the actual implementation rationale when this ticket lands: scope, technical
choices, alternatives and accepted trade-offs. Mirror this same paragraph verbatim
into DEV-LOG.md. Do not fabricate a completed rationale during planning.
