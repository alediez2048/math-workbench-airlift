# Claude Code handoff — September 16, 2026

## Update 2026-09-16 afternoon

Claude Code resumed implementation. The installed app is now the CargoCrew grab
diagnostic build (artifacts/qa/cargo-20260916-130202, SHA-256
44bee55e...4d0b), not the vendor experiment below. See DEV-LOG.md (newest entry)
and docs/qa/cargo-CC-P1-02.md for the physical check script. The test-runner stall was
root-caused to a dirty-scene Save alert; the wrapper now guards against it.

## Start here: implementation paused for handoff (historical)

Owner requested this handoff after prolonged failed interaction debugging. Do not
interpret old completed setup/onboarding evidence as acceptance of the current app.
No further implementation, installation, commit or push performed for this handoff.

Workspace: `/Users/jad/Desktop/math-workbench-airlift`, branch `unity-airlift`,
HEAD `fd7895f`. Working tree is substantially dirty (68 status entries before this
documentation update). Preserve all changes; no reset/clean/automatic commit.
Unity project is the `unity/` subfolder. Editor 6000.6.0f1, URP, Meta SDK 205,
OpenXR; actual headset is Quest **3S**. Use official Unity CLI/Pipeline.

## Owner's latest scope override

- Prioritize a playable core: open Cargo Crew, see a stable reachable board, grab
  a whole labeled 1, split into equal halves labeled 1/2, manipulate the pieces.
- Voice/AI, speech captions/replay/mute integration are explicitly deferred.
  Keep basic written instructions and mathematical labels. P1-03 is no longer
  a prerequisite for P1-04/P1-05. This is a local demo scope change, not contest
  AI eligibility clearance or authorization to submit.
- Launcher ticket P1-02.5 remains requested, but must not block arithmetic.
- Owner wants free controller repositioning of the station. Current controls
  only raise/lower/recenter; no drag handle exists. Treat as open requirement,
  not an implemented feature. Routine design decisions were delegated.
- No purchases, provider calls, child-data processing, commits/pushes or external
  submissions authorized by this change. Other two lesson cards stay disabled.

## Verified and owner-reported state

| Area | Actual state |
|---|---|
| Earlier DeviceProof/Onboarding | Owner previously grabbed successfully; historical evidence only. |
| Cargo visuals | Owner sees truck/arrivals, parcels/staging, aircraft/air delivery, two containers and likes the appearance. |
| Flying station | Diagnosed camera origin falling under FirstPersonLocomotor gravity. Disabled that component on CargoCrew only; owner confirms station now stays still. |
| Startup placement | Owner reports board initially on floor; system recenter helps. Initial Start samples head before tracking is ready. Cause of low placement not conclusively tested/fixed. |
| Practice grabbing | **FAIL**: owner read actual practice instructions, placed controller against strap, pressed buttons; no grab. Not merely attempting the non-interactive demo. |
| Fractions/AI | No playable fraction activity, splitting/merging, or AI guide implemented. Draft math and FractionNotationView code are not working lesson evidence. |
| Ticket acceptance | P1-01 and P1-02 not fully accepted. Reset-to-default, notation specimens/layout tests and other P1-02 gaps remain. |

## Installed app: isolated vendor test, NOT Cargo Crew

Last installation succeeded and cold launch succeeded for:

`artifacts/vendor-grab-proof.apk`

SHA256 `f0e5ed8f278493de7e8381b672dd783805200d3cedf85e938bbcaf9a05e82070`.
Bounded credential scan passed. Package remains `com.jad.airlift`, version 0.1.0,
code 1. This **replaced** the CargoCrew APK on the device; it did not erase project
scenes. ADB authorization was working at last check; recheck before deploying.

Owner sees the platform but **not the cube**. This experiment is **INVALID / not
accepted**: we discovered the supplied `[BB] Grabbable Cube` prefab contains
Grabbable plus HandGrabInteractable, but no controller GrabInteractable. Missing
cube visibility is unexplained; do not claim its cause is hand-only interaction.
Do not use failure of this scene as proof of a shared project/input fault.

Builder: `unity/AgentScripts/CreateVendorGrabProof.cs`.
Scene: `unity/Assets/Airlift/Scenes/VendorGrabProof.unity`.
Fresh Meta passthrough/controller blocks, supplied cube prefab, no lesson scripts.
Changes from vendor defaults: floor-level origin, transparent camera background,
FirstPersonLocomotor disabled for stationary safety, cube at (0,1,.55), static
platform at (0,.8,.6). Cube prefab has gravity off, kinematic on, trigger collider;
renderer enabled and default material. No evidence it fell due to gravity.
Scene builder checks one rig and a Grabbable but failed to check controller support
and rendered cube visibility. That validation gap must be corrected before retry.

## Useful runtime evidence from CargoCrew diagnostic

Cargo diagnostic APK (last CargoCrew build, available to restore):
`artifacts/qa/cargo-20260916-114658/airlift-cargo.apk`.
25 Unity checks passed; build and bounded credential scan passed; install/launch
succeeded; owner still could not grab. This build is not a fix.

Temporary `GrabProbe()` in OnboardingDirector samples every .5 seconds for at most
120 seconds after Start, only printing in Practice. Prefix `[DEBUG-cargo-grab]`.
Observed: practice strap active=true, target enabled=true, collider count=1.
Interactor reached Hover, candidate=true, distance=0.000–0.025m, selected=false.
All observed raw OVRInput left/right PrimaryHandTrigger readings were 0.00.
There are four ControllerGrabInteractor entries in the sample, with repeated
names; do not assume this means duplicate rigs (could be vendor modalities).

Interpretation: contact/candidate detection does work; selection does not.
Input/selector path is the next hypothesis, **not a confirmed root cause**.
OVRInput readings must be cross-checked against the SDK's actual controller data
source/OpenXR mapping; .5-second samples can miss brief presses. Owner was asked
to hold grip two seconds. Do not blame the user's button choice or change bindings
without evidence. Saved strap/collider and explicit selector blocks compared with
the historical Onboarding scene showed no relevant changes; this does not prove
runtime equivalence. No grab-specific exception was found in available ADB logs.

## Tests, tooling and unresolved hazards

- Latest full CargoCrew verification: 12 onboarding + 3 placement + 1 font binding
  + 1 glyph + 4 cargo layout + 3 baseline + 1 PlayMode scene = 25 passed.
- These tests do **not** establish real controller-grip selection or physical QA.
- First September 16 test run timed out in PlayMode; editor logged repeated
  ActiveStateTracker.Update null references. Exited play mode and requested script
  domain reload; repeat full run passed. Do not equate editor errors to headset cause.
- Recovery utility `unity/AgentScripts/ReloadAfterStalledTests.cs` uses
  EditorUtility.RequestScriptReload; no editor restart or unsaved scene overwrite.
- Temporary `[DEBUG-cargo-placement]` probes remain in OnboardingDirector and
  ComfortPlacement. Remove all temporary probes after diagnosis, then rebuild.
- Previous SDK developer-token injection is mitigated by ProofBuildSafety. Scan
  every APK before installing/sharing. Scanner handles IL2CPP v108 string boundaries;
  passing scan is bounded evidence, not universal proof of absence of secrets.
- Build warnings need triage; existing AssetPackManager ClassNotFoundException
  occurs at startup but app continues into focused OpenXR. Not proven causal.
- Several Unity processes/windows were observed. The tutorial project is separate.
  Screen capture repeatedly returned the tutorial while owner reported seeing
  CargoCrew. Use project-specific Pipeline targeting; do not trust capture as proof
  of which window the owner is seeing. Do not launch more editor copies.
- Airlift added to Unity Hub as project `unity`; correct path is Desktop repo/unity.
  CargoCrew was opened in that project's editor for owner inspection after the
  vendor build completed. Editor scene selection is not installed APK selection.

## Resume recommendation (not evidence of completed work)

1. Read this record and inspect local changes. Keep reference scenes and APKs.
2. Validate controller support/rendering in the vendor control scene before any
   new headset request; alternatively restore the diagnostic CargoCrew APK and
   trace the real SDK selector/data-source path with bounded instrumentation.
3. Fix a demonstrated cause; add a test at the relevant seam. Do not present a
   passing menu test as a grab regression test. Ask for one focused physical check.
4. Once grabbing works, implement the owner-approved whole/halves loop; do not
   resume voice/provider integration or polish as a prerequisite.
5. Record actual APK, tests, manual evidence, unresolved limits; no silent acceptance.

The prior agent repeatedly stopped at status explanations rather than completing
diagnostics, causing avoidable delay and frustration. Avoid new planning layers,
repeated ambiguous instructions, and claims of background progress without a running
operation. Build success and launch success are not proof the experience is usable.
